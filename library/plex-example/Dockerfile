#
# Metadata used by docker-build-and-push.yml GitHub Action Workflow
# IMAGE_NAME: plex-example
# IMAGE_VERSION: v0.0.1
#
# Random comment to trigger new hash

FROM busybox:stable

CMD [ "ps", "faux" ]
